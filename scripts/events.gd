extends Node

#warning-ignore-all:unused_signal
signal enemy_killed(enemy)

signal score_changed(score)

signal level_upped(new_level)

signal item_selected(item_data)

signal item_bought(item)

signal item_reseted

signal purchase_failed

signal star_added

signal difficulty_increased

signal scene_changed

signal fps_counter_toggled
